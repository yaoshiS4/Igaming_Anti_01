# Rules Manifest

> Lazy-load index for rules. Always load universal rules (anti-patterns, engineering-mindset). Load only your role's scoped rules — do NOT load all of them. Observe `context-budget.md` limits. See `instructions.md` §Context Optimization Protocol.
> Auto-generated from each rule's `description:` frontmatter.

| Rule | Description |
|---|---|
| `a11y-standards` | Always On — enforce WCAG accessibility compliance on every UI component |
| `agile-user-stories` | Always On — mandate standard user story format with acceptance criteria on every feature |
| `anti-patterns` | Always On — anti-hallucination, anti-loop, anti-context-overflow safety patterns + swarm coordination safety (loaded conditionally) |
| `anti-slop-ui` | Role-scoped (@designer, @dev-fe) — bans generic "SaaS slop" UI aesthetics; enforces high-agency premium visual design with explicit taste |
| `api-contract-first` | API Contract First - API contracts must be defined before implementation |
| `autonomous-tooling` | Always On — agents must proactively use tools without asking permission |
| `backend-standards` | Role-scoped (@dev-be, @sa, @devops) — single source of truth for backend quality constraints; load alongside the test-driven-development skill before every backend task |
| `code-standards` | Always On — DRY principle, strict TypeScript type safety, strict test coverage, and no-truncation |
| `context-budget` | Always On — context window budget tracking, warning thresholds, and progressive load management |
| `dialectical-development` | Dialectical Development — ideas must survive adversarial scrutiny before and after implementation |
| `documentation-standards` | Documentation Standards - SDLC output structure, code docs, and architecture diagram requirements |
| `engineering-mindset` | Always On — MVP-first, mobile-first, ruthless prioritization, and continuous improvement mindsets |
| `error-handling-standards` | Error Handling Standards - structured error types, boundaries, and user-facing messages |
| `execution-protocol` | Always On — verification gates, iteration budgets, commit discipline, agent coordination, and CLI worker governance |
| `git-hygiene` | Git Hygiene - conventional commits, atomic changes, and branch naming |
| `no-code-boundary` | Always On — code boundary between planning and execution roles in single-LLM persona-switching mode |
| `performance-budget` | Performance Budget - bundle size limits and Core Web Vitals thresholds |
| `routing` | Unified reference for task classification, confidence scoring, decision routing, and AI model selection by task type |
| `scalable-folder-structure` | Always On — enforce organized, scalable folder structure from project inception |
| `security-standards` | Always On — shift-left security, zero-trust secrets, and FOSS pipeline standards |
| `spawn-governance` | Pre-spawn validation and post-spawn timeout handling for CLI worker delegations |
| `ui-design-system` | Always On — enforce design token usage and prohibit invented colors, spacing, or typography |
| `user-feedback-format` | Always On — enforces standardized format for user feedback reports generated by @user-tester |
