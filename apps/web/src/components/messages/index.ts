/* ============================================================================
 * Yaobet — messages feature barrel (J9 消息中心 / Message Center). The /tin-nhan
 * inbox in the (member) area: an outer Card panel (header + filter + bulk
 * actions) over a stack of message cards, driven by getMessages(). All state is
 * client-only (UI-only / mock); truthful-only honest empty/guest/filtered states.
 * ==========================================================================*/

export { MessageCenter } from "./MessageCenter";
export type { MessageCenterProps } from "./MessageCenter";
